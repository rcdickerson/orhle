expected: valid;

forall: A;
exists: E;

pre: true;
post: (= A!y E!y);

especs:
 randOdd() {
   choiceVars: n;
   pre: (= (mod n 2) 1);
   post: (= ret! n);
 }

fun A() {
  x := 3;
  if x == 3 then
    y := 3;
  else
    y := 300;
  endif
  return y;
}

fun E() {
  x := call randOdd();
  if x == 3 then
    y := 3;
  else
    y := 300;
  endif
  return y;
}
