// A program together with an invalid refinement.

expected: invalid;

forall: p[refinement];
exists: p[original];

post: (= p!original!x p!refinement!x);

aspecs:
  randInt() {
      pre:  true;
      post: (and (>= ret! 0) (< ret! 25));
  }

especs:
  randInt() {
      templateVars: n;
      pre:  (and (>= n 0) (< n 20));
      post: (= ret! n);
}

prog p():
  x := call randInt();
endp